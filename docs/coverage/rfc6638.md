# RFC 6638 — Scheduling Extensions to CalDAV

**Test file:** `internal/suites/rfc6638/rfc6638.go`
**Tests registered:** 0 (suite not yet created)
**Requirements extracted:** ~55 (~45 HTTP-testable under `--tag scheduling`)

Coverage statuses:
- `conformance ✓` — all meaningful variations tested
- `lint only` — representative test exists; conformance variants missing
- `not covered` — no test exists
- `deferred` — intentionally out of scope with reason

> **Tag policy:** All tests in this suite are tagged `scheduling` — excluded from default protocol bundle runs. They execute when `--tag scheduling` is specified or when the server advertises `calendar-auto-schedule` in its `DAV` OPTIONS response. RFC 6638 requires RFC 4791 (`calendar-access`) as a prerequisite; rfc4791 suite tests must pass before running this suite.

---

## §2 — Capability Advertisement

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-01 | Server MUST include `calendar-auto-schedule` in the `DAV` OPTIONS response header | — | deferred |
| R-02 | Server MUST support `calendar-access` (RFC 4791) as a prerequisite | — | deferred |

**Proposed test:** `rfc6638.options-calendar-auto-schedule` — OPTIONS returns `calendar-auto-schedule` in `DAV` header.

---

## §2.1–2.2 — Scheduling Inbox and Outbox Collections

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-03 | Schedule-outbox `DAV:resourcetype` MUST include `DAV:collection` and `CALDAV:schedule-outbox` | — | deferred |
| R-04 | Schedule-outbox MUST NOT be a child (at any depth) of a calendar collection | — | deferred |
| R-05 | Schedule-inbox `DAV:resourcetype` MUST include `DAV:collection` and `CALDAV:schedule-inbox` | — | deferred |
| R-06 | Schedule-inbox MUST NOT be a child (at any depth) of a calendar collection | — | deferred |
| R-07 | Schedule-inbox MUST NOT contain collection resources | — | deferred |

**Proposed tests:**
- `rfc6638.inbox-resourcetype` — PROPFIND on the scheduling inbox returns `CALDAV:schedule-inbox` in `DAV:resourcetype`
- `rfc6638.outbox-resourcetype` — PROPFIND on the scheduling outbox returns `CALDAV:schedule-outbox` in `DAV:resourcetype`
- `rfc6638.inbox-not-nested-in-calendar` — scheduling inbox URL is not a descendant of any calendar collection

---

## §2.4.1 — calendar-user-address-set Property

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-08 | `CALDAV:calendar-user-address-set` property MUST be supported on principal resources | — | deferred |

**Proposed test:** `rfc6638.calendar-user-address-set` — PROPFIND on the current-user-principal returns `CALDAV:calendar-user-address-set` with at least one address.

---

## §3.2 — Organizer PUT / iTIP Processing

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-09 | On PUT of a scheduling object resource, server MUST inspect each `ATTENDEE` to determine whether to send a scheduling message | — | deferred |
| R-10 | Server MUST add a `SCHEDULE-STATUS` parameter to each `ATTENDEE` property for which it attempted delivery | — | deferred |
| R-11 | Server MUST NOT set `SCHEDULE-STATUS` on `ATTENDEE` properties for which it did not attempt delivery | — | deferred |
| R-12 | Server MUST ensure `DTSTAMP` is present and set to the current UTC time on each scheduling object resource | — | deferred |
| R-13 | Server MUST update `SEQUENCE` per iTIP rules on each scheduling operation | — | deferred |
| R-14 | On reschedule, server MUST reset `PARTSTAT` to `NEEDS-ACTION` for all rescheduled instances | — | deferred |
| R-15 | All calendar components in a scheduling object resource MUST contain the same `ORGANIZER` value | — | deferred |

**Proposed tests:**
- `rfc6638.put-with-attendee-sets-schedule-status` — PUT a VEVENT with an ATTENDEE; GET returns the resource with `SCHEDULE-STATUS` on the ATTENDEE property
- `rfc6638.put-reschedule-resets-partstat` — PUT updated VEVENT with changed DTSTART; GET shows all ATTENDEEs have `PARTSTAT=NEEDS-ACTION`
- `rfc6638.reject-mixed-organizer` — PUT iCal where two VEVENT components have different ORGANIZER values returns 4xx

---

## §3.2.2 — Attendee Modifications

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-16 | Server MUST allow attendees to update their own `PARTSTAT`, `RSVP`, `X-` properties, `COMMENT`, `TRANSP`, `DTSTART` (floating only), and alarm components | — | deferred |

**Proposed test:** `rfc6638.attendee-can-update-partstat` — attendee's PUT updating only `PARTSTAT=ACCEPTED` on their copy of the event returns 2xx.

---

## §3.2.5 — Recurrence Instance Restrictions

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-17 | Server MUST ensure attendees only receive recurrence instances that explicitly include them | — | deferred |

---

## §3.2.10 — SCHEDULE-TAG

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-18 | Successful GET or PUT on a scheduling object resource MUST include a `Schedule-Tag` response header | — | deferred |
| R-19 | Successful COPY or MOVE on a scheduling object resource MUST include a `Schedule-Tag` response header | — | deferred |
| R-20 | Server MUST NOT change `CALDAV:schedule-tag` when processing an attendee reply (participation status update only) | — | deferred |
| R-21 | Server MUST change `CALDAV:schedule-tag` when processing an organizer update that reschedules | — | deferred |
| R-22 | `If-Schedule-Tag-Match` header mismatch MUST result in 412 Precondition Failed | — | deferred |
| R-23 | When `If-Schedule-Tag-Match` succeeds, server MUST apply attendee changes from the stored resource to the new resource | — | deferred |

**Proposed tests:**
- `rfc6638.get-returns-schedule-tag` — GET on a scheduling object resource returns `Schedule-Tag` response header
- `rfc6638.put-returns-schedule-tag` — PUT a scheduling object resource returns `Schedule-Tag` response header
- `rfc6638.if-schedule-tag-match-stale-412` — PUT with `If-Schedule-Tag-Match: <old-tag>` returns 412 when the tag has changed
- `rfc6638.if-schedule-tag-match-current-succeeds` — PUT with `If-Schedule-Tag-Match: <current-tag>` returns 2xx

---

## §4 — Incoming Scheduling Message Processing

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-24 | Server MUST automatically process incoming scheduling messages per RFC 5546 rules | — | deferred |
| R-25 | Server MUST account for ACL privileges on scheduling Inbox when processing messages | — | deferred |
| R-26 | After auto-processing, scheduling message MUST appear in attendee's inbox once | — | deferred |
| R-27 | Server MUST update `PARTSTAT` on the attendee's calendar copy when a reply is processed | — | deferred |
| R-28 | Server MUST update or add `SCHEDULE-STATUS` on matching `ATTENDEE` when processing a reply | — | deferred |
| R-29 | Attendee MUST have at least one valid calendar collection; server MUST reject delivery if none available | — | deferred |

**Proposed tests:**
- `rfc6638.invite-appears-in-inbox` — organizer PUTs event with attendee; attendee's scheduling inbox contains a scheduling message
- `rfc6638.accept-updates-partstat` — attendee sends REPLY via POST to outbox; organizer's copy shows updated `PARTSTAT`

---

## §4.3 / §9.2 — Scheduling Properties

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-30 | `CALDAV:schedule-default-calendar-URL` MUST refer to a valid calendar collection belonging to the owner | — | deferred |
| R-31 | Server MUST reject deletion of the default calendar collection | — | deferred |

**Proposed tests:**
- `rfc6638.schedule-default-calendar-url-valid` — PROPPATCH sets `CALDAV:schedule-default-calendar-URL` to a non-existent URL returns 4xx
- `rfc6638.reject-delete-default-calendar` — DELETE on the calendar pointed to by `CALDAV:schedule-default-calendar-URL` returns 4xx

---

## §5 — Free-Busy Requests (POST to Outbox)

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-32 | POST target MUST be a `CALDAV:schedule-outbox` collection | — | deferred |
| R-33 | `ORGANIZER` in the `VFREEBUSY` MUST match a calendar user address of the outbox owner | — | deferred |
| R-34 | Response MUST be an XML document with `CALDAV:schedule-response` as the root element | — | deferred |

**Proposed tests:**
- `rfc6638.freebusy-post-to-outbox` — POST a VFREEBUSY request to the scheduling outbox returns 200 with `CALDAV:schedule-response` body
- `rfc6638.freebusy-post-wrong-organizer` — POST VFREEBUSY with ORGANIZER that doesn't match outbox owner returns 4xx

---

## §6 — Scheduling Privileges

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-35 | Scheduling privileges MUST be non-abstract and MUST appear in `DAV:supported-privilege-set` | — | deferred |
| R-36 | Server MUST aggregate scheduling privileges per the defined hierarchy | — | deferred |

**Proposed test:** `rfc6638.scheduling-privileges-in-supported-set` — PROPFIND on inbox/outbox returns `DAV:supported-privilege-set` that includes `CALDAV:schedule` and its sub-privileges.

---

## §7 — iCalendar Parameter Handling

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-37 | Server MUST NOT include `SCHEDULE-AGENT` in sent scheduling messages | — | deferred |
| R-38 | Server MUST NOT preserve `SCHEDULE-FORCE-SEND` parameter in stored scheduling object resources | — | deferred |
| R-39 | Server MUST NOT preserve `SCHEDULE-STATUS` in outgoing scheduling messages | — | deferred |

---

## Deferred

All requirements in this RFC are tagged `scheduling` — deferred from default protocol bundle runs.

| Group | Reason |
|-------|--------|
| All §2–§9 requirements | tagged `scheduling` — excluded from protocol bundle runs by default; runs when `--tag scheduling` is specified or discovery sees `calendar-auto-schedule` in DAV header |

---

## Test Rename Candidates

No existing tests — not applicable.

---

## Coverage Summary

| Status | Count |
|--------|-------|
| `conformance ✓` | 0 |
| `lint only` | 0 |
| `not covered` | 0 |
| `deferred` | ~39 (all requirements — tagged `scheduling`) |
| **Total** | **~39** |
