# RFC 7232 — HTTP/1.1: Conditional Requests

**Test file:** `internal/suites/rfc7232/rfc7232.go`
**Tests registered:** 16
**Requirements extracted:** ~33 (~20 directly HTTP-testable)

Coverage statuses:
- `conformance ✓` — all meaningful variations tested
- `lint only` — representative test exists; conformance variants missing
- `not covered` — no test exists
- `deferred` — intentionally out of scope with reason

> **Context:** RFC 7232 defines HTTP conditional request headers (`If-Match`, `If-None-Match`, `If-Modified-Since`, `If-Unmodified-Since`) and validator semantics (ETags, Last-Modified). In CalDAV/CardDAV these are critical for safe concurrent writes: clients MUST use `If-None-Match: *` when creating resources and `If-Match: <etag>` when updating, to prevent lost updates. RFC 6352 §5.3.4 and RFC 4791 §5.3.4 both require servers to enforce these semantics.

---

## §2.1 — Weak vs Strong Validators

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-01 | Server MUST only mark an ETag as weak (prefix `W/`) when it cannot satisfy strong validator characteristics | `rfc7232.etag-is-strong` | lint ✓ |
| R-02 | Server SHOULD update a weak ETag whenever the prior representation becomes an unacceptable substitute | — | deferred |

**Note (R-01):** RFC 6352 §5.3.4 independently requires strong ETags on address object resources. Testing that ETags are strong (no `W/` prefix) simultaneously covers both RFCs.

---

## §2.2.1 — Last-Modified Generation

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-03 | Server SHOULD include `Last-Modified` in responses for representations where modification dates can be determined | `rfc7232.last-modified-present` | lint ✓ |
| R-04 | Server MUST NOT send a `Last-Modified` date later than the `Date` header in the same response | — | deferred |
| R-05 | Server MUST replace future-dated modification times with the message origination time | — | deferred |

**Note (R-04, R-05):** Requires clock inspection and comparison between response headers — not reliably testable in black-box HTTP testing without time-manipulation facilities.

---

## §2.3.1 — ETag Generation

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-06 | Server SHOULD send ETags for representations where change detection is feasible | `rfc7232.etag-changes-after-put` | lint ✓ |
| R-07 | Server MAY send weak ETags when strong tags are infeasible or performance concerns apply | — | deferred |

---

## §2.4 — When to Use Validators in Responses

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-08 | Server SHOULD include a strong ETag in 200 responses to GET unless infeasible | `rfc7232.etag-is-strong` | lint ✓ |
| R-09 | Server SHOULD include `Last-Modified` in 200 responses to GET when feasible | `rfc7232.last-modified-present` | lint ✓ |
| R-10 | Server SHOULD include ETag in responses to PUT/POST that create or modify a representation | `rfc7232.etag-on-put-response` | lint ✓ |

---

## §3.1 — If-Match

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-11 | Server MUST use strong comparison when evaluating `If-Match` | `rfc7232.if-match-matching-etag-succeeds` | lint ✓ |
| R-12 | Server MUST NOT perform the request method if `If-Match` evaluates to false (no matching ETag) | `rfc7232.if-match-stale-etag-412` | lint ✓ |
| R-13 | Server MUST return 412 Precondition Failed when `If-Match` fails | `rfc7232.if-match-stale-etag-412` | lint ✓ |
| R-14 | `If-Match: *` MUST match any existing resource; MUST fail (412) when resource does not exist | `rfc7232.if-match-wildcard-existing-succeeds`, `rfc7232.if-match-wildcard-missing-412` | lint ✓ |

---

## §3.2 — If-None-Match

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-15 | Server MUST use weak comparison when evaluating `If-None-Match` | `rfc7232.if-none-match-get-cached-304` | lint ✓ |
| R-16 | Server MUST NOT perform the method if `If-None-Match` evaluates to false (ETag matched) | `rfc7232.if-none-match-get-cached-304`, `rfc7232.if-none-match-wildcard-existing-412` | lint ✓ |
| R-17 | For GET/HEAD, when `If-None-Match` fails, server MUST return 304 Not Modified | `rfc7232.if-none-match-get-cached-304` | lint ✓ |
| R-18 | For other methods (PUT, DELETE, etc.), when `If-None-Match` fails, server MUST return 412 | `rfc7232.if-none-match-wildcard-existing-412` | lint ✓ |
| R-19 | `If-None-Match: *` MUST fail (412) when the resource already exists; MUST succeed when resource does not exist | `rfc7232.if-none-match-wildcard-new-resource-201`, `rfc7232.if-none-match-wildcard-existing-412` | lint ✓ |

---

## §3.3 — If-Modified-Since

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-20 | Server MUST ignore `If-Modified-Since` when `If-None-Match` is also present | — | not covered |
| R-21 | Server MUST ignore `If-Modified-Since` for invalid date values | — | not covered |
| R-22 | Server MUST ignore `If-Modified-Since` for methods other than GET or HEAD | — | not covered |
| R-23 | Server SHOULD return 304 when the resource has not been modified since the given date | `rfc7232.if-modified-since-not-modified-304`, `rfc7232.if-modified-since-modified-200` | lint ✓ |

---

## §3.4 — If-Unmodified-Since

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-24 | Server MUST ignore `If-Unmodified-Since` when `If-Match` is also present | — | not covered |
| R-25 | Server MUST ignore `If-Unmodified-Since` for invalid date values | — | not covered |
| R-26 | Server MUST NOT perform the method if the resource has been modified more recently than the given date | `rfc7232.if-unmodified-since-modified-412`, `rfc7232.if-unmodified-since-not-modified-succeeds` | lint ✓ |
| R-27 | Server MUST return 412 when `If-Unmodified-Since` fails | `rfc7232.if-unmodified-since-modified-412` | lint ✓ |

---

## §4.1 — 304 Not Modified Response

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-28 | 304 response MUST include `ETag`, `Cache-Control`, `Content-Location`, `Date`, `Expires`, or `Vary` headers as would appear in a 200 response | — | not covered |
| R-29 | 304 response MUST NOT contain a message body | `rfc7232.if-none-match-get-cached-304` | lint ✓ |

---

## §5 — Precondition Evaluation

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-30 | Server MUST evaluate preconditions after normal request checks, before the method action | — | deferred |
| R-31 | Server MUST ignore preconditions if response without them would be non-2xx/non-412 (e.g. 404 on a resource that never existed) | — | not covered |
| R-32 | Server MUST ignore conditional headers for CONNECT, OPTIONS, and TRACE | — | not covered |

**Note (R-30):** Evaluation ordering is implementation-internal and not observable in black-box testing.

---

## §6 — Precedence Order

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-33 | Server MUST evaluate preconditions in order: `If-Match` → `If-Unmodified-Since` → `If-None-Match` → `If-Modified-Since` → `If-Range` | — | not covered |

---

## Deferred

| Req | Requirement | Reason |
|-----|-------------|--------|
| R-02 | SHOULD update weak ETags when prior representation becomes unacceptable substitute | Implementation-internal; observable effect not distinguishable in black-box testing |
| R-04 | MUST NOT send Last-Modified later than response Date | Requires sub-second clock comparison between response headers; not reliably testable |
| R-05 | MUST replace future modification dates with message origination time | Requires clock manipulation or observation of future-timestamped resources |
| R-07 | MAY send weak ETags when strong tags are infeasible | MAY — no server obligation; not testable |
| R-30 | MUST evaluate preconditions after normal checks, before method action | Implementation-internal evaluation ordering |

---

## Coverage Summary

| Status | Count |
|--------|-------|
| `lint ✓` | 16 (R-01, R-03, R-06, R-08–R-19, R-23, R-26, R-27, R-29) |
| `not covered` | 12 (R-20–R-22, R-24–R-25, R-28, R-31–R-33) |
| `deferred` | 5 (R-02, R-04, R-05, R-07, R-30) |
| **Total** | **33** |
