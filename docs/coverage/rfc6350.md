# RFC 6350 — vCard Format Specification (vCard 4.0)

**Test file:** `internal/suites/rfc6350/rfc6350.go`
**Tests registered:** 2
**Requirements extracted:** ~35 (~20 directly HTTP-testable)

Coverage statuses:
- `conformance ✓` — all meaningful variations tested
- `lint only` — representative test exists; conformance variants missing
- `not covered` — no test exists
- `deferred` — intentionally out of scope with reason

> **Scope note:** RFC 6350 defines the vCard 4.0 format. Like RFC 2426, tests here validate server behaviour observable via HTTP: rejecting malformed vCards on PUT and verifying correct content on GET. Pure authoring rules (how a sender MUST format data it produces) are deferred unless a server is expected to validate them on receipt.

---

## §3.1 — Character Set (UTF-8)

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-01 | vCard charset MUST be UTF-8; no override is defined | — | not covered |
| R-02 | MUST NOT specify a charset other than `UTF-8` in the `text/vcard` MIME parameter | — | not covered |

**Missing tests:**
- `rfc6350.put-charset-utf8-accepted` — PUT with `Content-Type: text/vcard; charset=utf-8` returns 2xx
- `rfc6350.put-charset-latin1-rejected` — PUT with `Content-Type: text/vcard; charset=iso-8859-1` returns 4xx (unsupported charset)

---

## §3.2 — Line Delimiting and Folding

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-03 | Lines MUST be delimited by CRLF | — | not covered |
| R-04 | Lines longer than 75 octets SHOULD be folded | — | not covered |
| R-05 | Multi-octet characters MUST NOT be split across fold boundaries | — | deferred |
| R-06 | CRLF followed by a single white space character (SP or HT) MUST be unfolded before parsing | — | not covered |

**Note (R-05):** Implementation-internal; not observable via HTTP response inspection.

**Missing tests:**
- `rfc6350.folded-line-parsed` — PUT a vCard with a CRLF+SP fold in the FN line; GET returns intact FN value (parallel to `rfc2426.folded-line-parsed`)
- `rfc6350.folded-line-tab-parsed` — PUT a vCard with a CRLF+HT fold; GET returns intact FN value

---

## §3.3 — Required Properties and VERSION Placement

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-07 | vCard object MUST include VERSION and FN properties | — | not covered |
| R-08 | VERSION MUST appear immediately after BEGIN:VCARD | — | not covered |

**Missing tests:**
- `rfc6350.reject-missing-fn` — PUT a vCard 4.0 without FN is rejected with 4xx
- `rfc6350.reject-missing-version` — PUT a vCard 4.0 without VERSION is rejected with 4xx
- `rfc6350.reject-version-not-first` — PUT a vCard where another property precedes VERSION is rejected with 4xx (or normalised correctly)

---

## §3.4 — Property Value Escaping

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-09 | COMMA in values MUST be backslash-escaped (even in non-compound properties) | — | not covered |
| R-10 | SEMICOLON in compound property fields MUST be backslash-escaped | — | not covered |
| R-11 | BACKSLASH characters in values MUST be escaped as `\\` | — | not covered |
| R-12 | Newline (U+000A) in values MUST be encoded as `\n` or `\N` | — | not covered |

**Missing tests:**
- `rfc6350.comma-escape-roundtrip` — PUT a vCard with an escaped `,` in FN; GET returns FN with the literal comma preserved
- `rfc6350.semicolon-escape-roundtrip` — PUT a vCard with an escaped `;` in N component; GET returns intact value
- `rfc6350.backslash-escape-roundtrip` — PUT a vCard with `\\` in NOTE; GET returns a literal backslash

---

## §6.1.1 / §6.1.2 — BEGIN and END

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-13 | vCard MUST begin with `BEGIN:VCARD` | — | not covered |
| R-14 | vCard MUST end with `END:VCARD` | — | not covered |

**Missing tests:**
- `rfc6350.reject-missing-begin` — PUT a body without `BEGIN:VCARD` is rejected with 4xx
- `rfc6350.reject-missing-end` — PUT a body without `END:VCARD` is rejected with 4xx

---

## §6.1.4 — KIND Property

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-15 | MEMBER properties MUST NOT appear unless `KIND` value is `group` | — | not covered |

**Missing test:**
- `rfc6350.reject-member-without-group-kind` — PUT a vCard with a MEMBER property but KIND≠group is rejected with 4xx

---

## §6.2.1 — FN (Formatted Name)

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-16 | FN MUST be present in every vCard object | — | not covered |

*(Covered by R-07 / `reject-missing-fn` above.)*

---

## §6.3.1 — ADR (Delivery Address)

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-17 | Component separators MUST be present even when component values are absent | — | not covered |

**Missing test:**
- `rfc6350.adr-structure-roundtrip` — PUT a vCard with a sparse ADR (some components blank); GET returns ADR with correct separator count

---

## §6.7.6 — UID Property

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-18 | UID MUST uniquely identify the vCard object | `rfc6350.uid-assignment` | lint only |

**Conformance gap:** `uid-assignment` verifies the server assigns a UID when one is absent, but does not verify that the server rejects a PUT with a UID that already exists on a different resource (uniqueness enforcement).

**Missing test:**
- `rfc6350.uid-uniqueness-enforced` — PUT two vCards with the same explicit UID on different URLs; second PUT returns 4xx (conflict)

---

## §6.7.9 — VERSION Property

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-19 | VERSION MUST be present in every vCard object | — | not covered |
| R-20 | VERSION MUST appear immediately after BEGIN:VCARD | — | not covered |
| R-21 | VERSION value MUST be `4.0` for vCard 4.0 compliance | `rfc6350.get-default-v4` | lint only |

**Conformance gap (R-21):** The existing tests verify the server *serves* VERSION:4.0 on GET, but do not verify the server *rejects* a PUT with VERSION:3.0 or VERSION:2.1 in the 4.0 context.

**Missing tests:**
- `rfc6350.reject-missing-version` — PUT a vCard 4.0 without VERSION is rejected (see also R-07)
- `rfc6350.reject-invalid-version` — PUT a vCard with VERSION:3.0 targeted at a v4.0-only endpoint is rejected with 4xx

---

## §5.3 — PREF Parameter

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-22 | PREF value MUST be an integer between 1 and 100 | — | not covered |

**Missing test:**
- `rfc6350.reject-pref-out-of-range` — PUT a vCard with `TEL;PREF=0:...` or `TEL;PREF=101:...` is rejected with 4xx

---

## Deferred

| Req | Requirement | Reason |
|-----|-------------|--------|
| R-05 | Multi-octet characters MUST NOT be split across fold boundaries | Implementation-internal; not observable in HTTP responses |
| §4.3 | Dates/times MUST use ISO 8601 basic format | Format parsing rule; server enforcement not specified — testable but low priority |
| §4.5/§4.6 | INTEGER bounded to signed 64-bit; FLOAT to IEEE binary64 precision | Implementation-internal numeric constraints |
| §5.1 | LANGUAGE parameter — no default language concept | Parameter semantics; not a server conformance test |
| §5.4 | ALTID MUST NOT appear on single-instance properties | Client-side authoring rule |
| §5.5 | CLIENTPIDMAP / PID source identifiers (§6.7.7) | Synchronisation protocol; client-side rule |
| §7.1 | vCard instance matching, property instance matching, PID matching | Synchronisation/merge protocol; not a server HTTP conformance test |
| §6.1.5 | XML property MUST be preserved when propagating vCards | Propagation/storage behaviour; not directly observable via simple GET round-trip |

---

## Coverage Summary

| Status | Count |
|--------|-------|
| `conformance ✓` | 0 |
| `lint only` | 2 (R-18, R-21) |
| `not covered` | 19 (R-01–R-04, R-06–R-17, R-19–R-20, R-22) |
| `deferred` | 9 |
| **Total** | **30** |
