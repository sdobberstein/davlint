# RFC 3744 — WebDAV Access Control Protocol

**Test file:** `internal/suites/rfc3744/rfc3744.go`
**Tests registered:** 0 (suite not yet created)
**Requirements extracted:** ~60 (~45 HTTP-testable under `--tag acl`)

Coverage statuses:
- `conformance ✓` — all meaningful variations tested
- `lint only` — representative test exists; conformance variants missing
- `not covered` — no test exists
- `deferred` — intentionally out of scope with reason

> **Tag policy:** All tests in this suite are tagged `acl` — excluded from default protocol bundle runs. They execute when `--tag acl` is specified or when the server advertises `access-control` in its `DAV` OPTIONS response. The entire suite is deferred below; proposed test IDs document what to implement when the tag is enabled.

---

## §7.2 — Capability Advertisement

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-01 | Server MUST include `access-control` in the `DAV` response header if it supports ACL | — | deferred |

**Proposed test:** `rfc3744.options-access-control` — OPTIONS returns `access-control` in `DAV` header.

---

## §3 — Privilege Model

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-02 | Access to a method MUST be controlled by one or more privileges | — | deferred |
| R-03 | Server MUST return 403 Forbidden when access is denied | — | deferred |
| R-04 | Server MUST return `DAV:error` / `DAV:need-privileges` body on 403 | — | deferred |
| R-05 | A privilege MUST NOT contain itself (no circular privilege trees) | — | deferred |
| R-06 | Custom privileges MUST NOT use the `DAV:` namespace | — | deferred |

**Proposed tests:**
- `rfc3744.denied-request-returns-403` — request on a resource without the required privilege returns 403
- `rfc3744.denied-403-body-need-privileges` — 403 response body contains `DAV:error` with `DAV:need-privileges`

---

## §3.12 — Standard Privilege Aggregation

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-07 | `DAV:write` MUST contain `DAV:bind`, `DAV:unbind`, `DAV:write-content`, `DAV:write-properties` | — | deferred |
| R-08 | `DAV:read-acl` MUST NOT contain `DAV:read`, `DAV:write`, or `DAV:write-acl` | — | deferred |
| R-09 | `DAV:write-acl` MUST NOT contain `DAV:write`, `DAV:read`, or `DAV:read-acl` | — | deferred |
| R-10 | `DAV:read-current-user-privilege-set` MUST NOT contain `DAV:write` or `DAV:read` | — | deferred |

**Proposed test:** `rfc3744.supported-privilege-aggregation` — PROPFIND `DAV:supported-privilege-set` shows correct parent-child relationships for standard privileges.

---

## §4 — Principal Resources

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-11 | Principal resources MUST be exposed at an `http(s)` URL | — | deferred |
| R-12 | Principal resources MUST report `DAV:principal` in `DAV:resourcetype` | — | deferred |
| R-13 | Principals MUST have a non-empty `DAV:displayname` property | — | deferred |
| R-14 | `DAV:alternate-URI-set` property MUST be supported on principals | — | deferred |
| R-15 | `DAV:principal-URL` property MUST be supported on principals | — | deferred |
| R-16 | `DAV:group-membership` property MUST be supported on principals | — | deferred |

**Proposed tests:**
- `rfc3744.principal-resourcetype` — PROPFIND on the current-user-principal returns `DAV:principal` in `DAV:resourcetype`
- `rfc3744.principal-displayname` — PROPFIND on principal returns non-empty `DAV:displayname`
- `rfc3744.principal-url-property` — PROPFIND on principal returns `DAV:principal-URL`
- `rfc3744.principal-group-membership` — PROPFIND on principal returns `DAV:group-membership`
- `rfc3744.principal-alternate-uri-set` — PROPFIND on principal returns `DAV:alternate-URI-set`

---

## §5 — Access Control Properties

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-17 | Resources supporting ACL MUST contain `DAV:owner`, `DAV:group`, `DAV:supported-privilege-set`, `DAV:current-user-privilege-set`, `DAV:acl`, `DAV:acl-restrictions`, `DAV:inherited-acl-set`, `DAV:principal-collection-set` | — | deferred |
| R-18 | These properties MUST NOT be present on null resources | — | deferred |
| R-19 | These properties SHOULD NOT be returned by `PROPFIND allprop` | — | deferred |
| R-20 | Abstract privileges MUST NOT appear in an ACE | — | deferred |
| R-21 | Server MUST reject attempts to set abstract privileges | — | deferred |
| R-22 | `DAV:supported-privilege-set` descriptions MUST include `xml:lang` attribute | — | deferred |
| R-23 | `DAV:current-user-privilege-set` elements MUST identify non-abstract privileges | — | deferred |
| R-24 | Protected ACEs MUST NOT be removable; attempt MUST fail | — | deferred |

**Proposed tests:**
- `rfc3744.supported-privilege-set-present` — PROPFIND returns `DAV:supported-privilege-set`
- `rfc3744.current-user-privilege-set-present` — PROPFIND returns `DAV:current-user-privilege-set`
- `rfc3744.acl-property-present` — PROPFIND returns `DAV:acl`
- `rfc3744.acl-not-in-allprop` — `PROPFIND allprop` does not include `DAV:acl` or `DAV:current-user-privilege-set`
- `rfc3744.reject-set-abstract-privilege` — ACL request granting an abstract privilege returns 403 with `DAV:no-abstract`
- `rfc3744.reject-remove-protected-ace` — ACL request removing a protected ACE returns 403 with `DAV:no-protected-ace-conflict`

---

## §8.1 — ACL Method

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-25 | ACL method MUST only permit modification to non-inherited, non-protected ACEs | — | deferred |
| R-26 | ACL request body MUST contain exactly one `DAV:acl` element | — | deferred |
| R-27 | `DAV:grant` and `DAV:deny` elements MUST only contain non-abstract privilege elements | — | deferred |

**Proposed test:** `rfc3744.acl-modifies-permissions` — ACL request grants read privilege to a second user; that user can subsequently GET the resource.

---

## §8.1.1 — ACL Precondition Codes

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-28 | `DAV:no-protected-ace-conflict` — ACEs MUST NOT conflict with protected ACEs | — | deferred |
| R-29 | `DAV:no-inherited-ace-conflict` — ACEs MUST NOT conflict with inherited ACEs | — | deferred |
| R-30 | `DAV:limited-number-of-aces` — ACE count MUST NOT exceed server limit | — | deferred |
| R-31 | `DAV:deny-before-grant` — deny ACEs MUST precede grant ACEs (when enforced) | — | deferred |
| R-32 | `DAV:grant-only` — deny ACEs MUST NOT be used (when restriction applies) | — | deferred |
| R-33 | `DAV:no-invert` — `DAV:invert` MUST NOT appear (when restriction applies) | — | deferred |
| R-34 | `DAV:no-abstract` — abstract privileges MUST NOT be granted or denied | — | deferred |
| R-35 | `DAV:recognized-principal` — every principal URL in ACL MUST identify a principal resource | — | deferred |
| R-36 | On precondition violation, server MUST return 403 or 409 with `DAV:error` body | — | deferred |

**Proposed tests:**
- `rfc3744.acl-reject-unrecognized-principal` — ACL granting privilege to a non-existent principal URL returns 4xx with `DAV:recognized-principal`
- `rfc3744.acl-reject-abstract-privilege` — ACL granting an abstract privilege returns 4xx with `DAV:no-abstract`
- `rfc3744.acl-reject-inherited-conflict` — ACL conflicting with an inherited ACE returns 4xx with `DAV:no-inherited-ace-conflict`

---

## §7.3–7.4 — COPY and MOVE Interaction

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-37 | MOVE MUST NOT modify the non-inherited, non-protected ACEs on the moved resource | — | deferred |
| R-38 | COPY destination `DAV:acl` MUST be the same as if the resource were individually created at that location | — | deferred |

**Proposed tests:**
- `rfc3744.move-preserves-acl` — MOVE a resource; PROPFIND on destination shows same ACL as source
- `rfc3744.copy-inherits-destination-acl` — COPY a resource; PROPFIND on destination shows the destination's inherited ACL, not source ACL

---

## §9 — ACL REPORT Methods

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-39 | Server MUST support `DAV:expand-property` report | — | deferred |
| R-40 | Server MUST support `DAV:acl-principal-prop-set` report (Depth: 0 only; no duplicate principal properties) | — | deferred |
| R-41 | Server MUST support `DAV:principal-match` report | — | deferred |
| R-42 | Server MUST support `DAV:principal-property-search` report | — | deferred |
| R-43 | Server MUST support `DAV:principal-search-property-set` report | — | deferred |

**Proposed tests:**
- `rfc3744.acl-principal-prop-set` — `acl-principal-prop-set` report on a resource returns principal properties without duplicates
- `rfc3744.principal-match` — `principal-match` report returns resources where the current user is a principal
- `rfc3744.principal-property-search` — `principal-property-search` on displayname returns matching principals
- `rfc3744.principal-search-property-set` — `principal-search-property-set` report returns searchable properties

---

## Deferred

All requirements in this RFC are tagged `acl` — deferred from default protocol bundle runs.

| Group | Reason |
|-------|--------|
| All §3–§9 requirements | tagged `acl` — excluded from protocol bundle runs by default; runs when `--tag acl` is specified or discovery sees `access-control` in DAV header |

---

## Test Rename Candidates

No existing tests — not applicable.

---

## Coverage Summary

| Status | Count |
|--------|-------|
| `conformance ✓` | 0 |
| `lint only` | 0 |
| `not covered` | 0 |
| `deferred` | ~43 (all requirements — tagged `acl`) |
| **Total** | **~43** |
