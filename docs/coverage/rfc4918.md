# RFC 4918 — HTTP Extensions for Web Distributed Authoring and Versioning (WebDAV)

**Test file:** `internal/suites/rfc4918/rfc4918.go`
**Tests registered:** 15
**Requirements extracted:** ~110 (~85 testable)

Coverage statuses:
- `conformance ✓` — all meaningful variations tested
- `lint only` — representative test exists; conformance variants missing
- `not covered` — no test exists
- `deferred` — intentionally out of scope with reason

---

## §9.1 — PROPFIND

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| 9.1-1 | All DAV resources MUST support PROPFIND | `rfc4918.propfind-depth0` | lint only |
| 9.1-2 | MUST support Depth:0 and Depth:1 | `rfc4918.propfind-depth0`, `rfc4918.propfind-depth1` | lint only |
| 9.1-3 | SHOULD support Depth:infinity | `rfc4918.propfind-depth-infinity-forbidden` — tests that it's **rejected** (403); tests the CardDAV-appropriate behavior but not the general SHOULD | lint only |
| 9.1-5 | SHOULD treat absent Depth as infinity | — | not covered |
| 9.1-6 | Empty body MUST be treated as allprop | — | not covered |
| 9.1-7 | Response Content-Type MUST be text/xml or application/xml | — | not covered |
| 9.1-8 | Non-existent property request MUST return 404 in propstat | — | not covered |
| 9.1-9 | Collection Depth:1 MUST include response per member | `rfc4918.propfind-depth1` | lint only |
| 9.1-11 | allprop MUST return all dead + RFC 4918 live properties | `rfc4918.propfind-allprop` | lint only |

**Notes:**
- `rfc4918.propfind-depth-infinity-forbidden` reflects CardDAV behavior (MUST reject infinity per RFC 4918 §9.1 + CardDAV guidance). Rename candidate: `rfc4918.propfind-depth-infinity-rejected` to make intent clearer.
- Missing test for `propfind-propname` (§9.1 defines three request variants: allprop, propname, named props — only two are tested).

---

## §9.2 — PROPPATCH

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| 9.2-1 | All DAV resources MUST support PROPPATCH | `rfc4918.proppatch-set` | lint only |
| 9.2-2 | Instructions MUST be processed in document order | — | not covered |
| 9.2-3 | Instructions MUST be atomic (all or none) | — | not covered |
| 9.2-4 | Response MUST be 207 with per-property status | `rfc4918.proppatch-set` | lint only |
| 9.2-5 | Failure to change property MUST report error in 207 propstat | — | not covered |
| 9.2-6 | Dependent properties MUST get 424 Failed Dependency | — | not covered |
| 9.2-7 | MUST NOT return 200 (only within 207) | — | not covered |

**Missing tests:**
- `rfc4918.proppatch-remove` — PROPPATCH remove deletes a dead property; PROPFIND returns 404 for it
- `rfc4918.proppatch-protected` — attempt to set a protected property returns 403 in propstat
- `rfc4918.proppatch-atomic` — one valid + one protected property; verify neither changes (atomicity)
- `rfc4918.proppatch-failed-dependency` — mix of valid/invalid; verify 424 on the dependent property

---

## §9.3 — MKCOL

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| 9.3-1 | MKCOL with body MUST return 415 | — | not covered |
| 9.3-2 | Success MUST return 201 | `rfc4918.mkcol` | conformance ✓ |
| 9.3-3 | Existing resource MUST return 405 | `rfc4918.mkcol-duplicate` | conformance ✓ |
| 9.3-4 | Missing parent MUST return 409 | — | not covered |
| 9.3-5 | MUST NOT create intermediate collections | — | not covered |

**Missing tests:**
- `rfc4918.mkcol-with-body` — MKCOL with request body returns 415
- `rfc4918.mkcol-missing-parent` — MKCOL with non-existent parent returns 409

---

## §9.4 — GET / HEAD

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| 9.4-1 | GET on collection MUST return 2xx (not 405) | — | not covered |
| 9.4-2 | GET/HEAD MUST follow HTTP/1.1 semantics | `rfc4918.get-contact` | lint only |

**Notes:** `rfc4918.get-contact` verifies Content-Type on a resource. Collection GET is not tested.

---

## §9.6 — DELETE

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| 9.6-1 | DELETE on collection MUST recursively delete all members | — | not covered |
| 9.6-2 | Success MUST return 204 | `rfc4918.delete` | conformance ✓ |
| 9.6-3 | Partial failure MAY return 207 Multi-Status | — | not covered |
| 9.6-4 | MUST NOT partially delete if errors occur | — | not covered |

**Missing tests:**
- `rfc4918.delete-collection` — DELETE non-empty collection; verify all members gone (404)

---

## §9.7 — PUT

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| 9.7-1 | PUT to unmapped URL MUST create resource (201) | `rfc4918.put-contact` | conformance ✓ |
| 9.7-2 | PUT to existing URL MUST update resource (200/204) | — | not covered |
| 9.7-3 | PUT MUST NOT create/modify collection (405) | — | not covered |
| 9.7-4 | PUT with missing parent MUST return 409 | — | not covered |

**Missing tests:**
- `rfc4918.put-update` — PUT to existing URL returns 200 or 204
- `rfc4918.put-to-collection` — PUT to collection URL returns 405
- `rfc4918.put-missing-parent` — PUT with non-existent parent returns 409

---

## §9.8 — COPY

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| 9.8-1 | COPY MUST NOT duplicate write locks | — | not covered |
| 9.8-2 | Dead properties MUST be duplicated | — | not covered |
| 9.8-3 | Live properties MUST be reset at destination | — | not covered |
| 9.8-4 | COPY of collection MUST copy all members | — | not covered |
| 9.8-5 | COPY to same URL MAY return 403 | — | not covered |
| 9.8-6 | Overwrite:F with existing destination MUST return 412 | — | not covered |
| 9.8-7 | Overwrite:T with existing destination MUST DELETE then copy | — | not covered |
| 9.8-8 | Absent Overwrite MUST behave as Overwrite:T | — | not covered |
| 9.8-9 | New destination MUST return 201; overwrite MUST return 204 | `rfc4918.copy` | lint only |
| 9.8-10 | Partial failure MUST return 207 | — | not covered |
| 9.8-11 | COPY Depth:0 MUST NOT copy collection members | — | not covered |

**Notes:** `rfc4918.copy` only tests the happy path (new destination, 201). All overwrite and property-preservation variants are missing.

**Missing tests:**
- `rfc4918.copy-overwrite-false` — Overwrite:F to existing destination returns 412
- `rfc4918.copy-overwrite-true` — Overwrite:T to existing destination returns 204
- `rfc4918.copy-overwrite-default` — no Overwrite header to existing destination returns 204
- `rfc4918.copy-depth-zero` — COPY collection with Depth:0 creates empty collection at destination
- `rfc4918.copy-preserves-dead-props` — dead properties present at destination after COPY
- `rfc4918.copy-no-lock-duplication` — destination not locked after COPY of locked source

---

## §9.9 — MOVE

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| 9.9-1 | Dead properties MUST be preserved at destination | — | not covered |
| 9.9-2 | Write locks MUST NOT move with resource | — | not covered |
| 9.9-4 | MOVE collection MUST move all members | — | not covered |
| 9.9-5 | Overwrite:F with existing destination MUST return 412 | — | not covered |
| 9.9-6 | Overwrite:T with existing destination MUST DELETE then move | — | not covered |
| 9.9-7 | Absent Overwrite MUST behave as Overwrite:T | — | not covered |
| 9.9-8 | New destination MUST return 201; overwrite MUST return 204 | `rfc4918.move` | lint only |
| 9.9-9 | Source MUST NOT exist after successful MOVE | `rfc4918.move` | conformance ✓ |
| 9.9-10 | Partial failure MUST return 207; source NOT partially deleted | — | not covered |

**Missing tests:**
- `rfc4918.move-overwrite-false` — Overwrite:F to existing destination returns 412
- `rfc4918.move-overwrite-true` — Overwrite:T to existing destination returns 204
- `rfc4918.move-overwrite-default` — no Overwrite header to existing destination returns 204
- `rfc4918.move-preserves-dead-props` — dead properties present at destination after MOVE
- `rfc4918.move-no-lock-transfer` — source lock removed and destination not locked after MOVE

---

## §9.10 — LOCK

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| 9.10-1 | Success MUST include Lock-Token response header | — | not covered |
| 9.10-2 | Success body MUST contain DAV:lockdiscovery | — | not covered |
| 9.10-3 | LOCK on unmapped URL MUST create locked resource (201) | — | not covered |
| 9.10-5 | MUST NOT grant conflicting lock | — | not covered |
| 9.10-6 | Depth-infinity LOCK conflicting with member MUST return 423 | — | not covered |
| 9.10-7 | New resource added to locked collection MUST be auto-locked | — | not covered |
| 9.10-8 | LOCK refresh MUST NOT create new lock | — | not covered |
| 9.10-10 | Class 2 resource MUST support LOCK | — | not covered |

**Note:** Locking is an optional feature. All locking tests are tagged `locking` and excluded from protocol bundle runs by default. They run when `--tag locking` is specified explicitly, or when discovery mode sees `2` in the DAV header.

---

## §9.11 — UNLOCK

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| 9.11-1 | UNLOCK without Lock-Token MUST return 400 | — | not covered |
| 9.11-2 | Success MUST return 204 | — | not covered |
| 9.11-3 | Wrong token MUST return 409 | — | not covered |
| 9.11-4 | Unauthorized UNLOCK MUST return 403 | — | not covered |
| 9.11-5 | Depth-infinity lock removal MUST unlock all members | — | not covered |

**Note:** Tagged `locking` — excluded from protocol bundle runs by default.

---

## §10.1 — DAV Header

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| 10.1-1 | DAV header MUST be present in OPTIONS response | `rfc4918.options` | conformance ✓ |
| 10.1-2 | Class 1 server MUST include `1` in DAV header | `rfc4918.options` | conformance ✓ |
| 10.1-3 | Class 2 server MUST include `2` in DAV header | `rfc4918.options` | conformance ✓ |

---

## §10.2 — Depth Header

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| 10.2-2 | Servers MUST support Depth:0 and Depth:1 | `rfc4918.propfind-depth0`, `rfc4918.propfind-depth1` | lint only |
| 10.2-4 | Absent Depth on PROPFIND MUST be treated as infinity | — | not covered |

---

## §10.3 — Destination Header

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| 10.3-2 | COPY/MOVE without Destination MUST return 400 | — | not covered |
| 10.3-4 | COPY/MOVE to different server MUST return 502 | — | not covered |

**Missing tests:**
- `rfc4918.copy-no-destination` — COPY without Destination header returns 400
- `rfc4918.move-no-destination` — MOVE without Destination header returns 400

---

## §10.4 — If Header

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| 10.4-1 | Write to locked resource without If header MUST return 423 | — | not covered |
| 10.4-2 | Failed If condition MUST return 412 | `rfc4918.delete-etag-mismatch` | lint only |
| 10.4-3 | Lock tokens in If MUST match actual locks | — | not covered |

**Notes:** `rfc4918.delete-etag-mismatch` tests If-Match on DELETE only. If-Match on PUT, If-None-Match, and lock token submission are not covered.

**Missing tests:**
- `rfc4918.put-if-match` — PUT with matching If-Match ETag succeeds
- `rfc4918.put-if-match-mismatch` — PUT with mismatched If-Match returns 412
- `rfc4918.put-if-none-match-star` — PUT with If-None-Match:* returns 412 if resource exists

---

## §10.5 — Lock-Token Header

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| 10.5-1 | LOCK response MUST include Lock-Token header | — | not covered |
| 10.5-2 | UNLOCK MUST include Lock-Token header | — | not covered |

**Note:** Excluded by default via `--exclude-tag locking`.

---

## §10.6 — Overwrite Header

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| 10.6-2 | Absent Overwrite MUST be treated as T | — | not covered |
| 10.6-3 | Overwrite:F with existing destination MUST return 412 | — | not covered |

(See COPY/MOVE sections above for associated test IDs.)

---

## §8 — General Requirements

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| 8.2-5 | Malformed XML body MUST return 400 | — | not covered |
| 8.3-1 | href values in Multi-Status MUST use consistent format | — | not covered |
| 8.3-4 | Collection identifiers SHOULD end with `/` | — | not covered |
| 8.5-1 | Auth checks MUST precede conditional header checks | — | not covered |
| 8.6-1 | ETag SHOULD NOT change if body/location unchanged | — | not covered |
| 8.6-3 | ETag MUST NOT be reused for different representations | — | not covered |

---

## §15 — Live Properties

| Req | Property | Current Test | Status |
|-----|----------|--------------|--------|
| 15.4 | DAV:getcontentlength MUST match Content-Length on GET | `rfc4918.propfind-depth0` | lint only |
| 15.6 | DAV:getetag MUST match ETag header on GET | `rfc4918.etag-on-get` | lint only |
| 15.7 | DAV:getlastmodified MUST be in RFC 1123 format | — | not covered |
| 15.7 | DAV:getlastmodified MUST match Last-Modified header on GET | — | not covered |
| 15.9 | DAV:resourcetype MUST be present; collections MUST have `<collection/>` | — | not covered |

**Missing tests:**
- `rfc4918.resourcetype-collection` — PROPFIND on collection; verify DAV:resourcetype contains `<collection/>`
- `rfc4918.resourcetype-resource` — PROPFIND on non-collection; verify DAV:resourcetype is empty
- `rfc4918.getetag-matches-get` — DAV:getetag value must equal ETag header returned by GET
- `rfc4918.getlastmodified-format` — DAV:getlastmodified must be RFC 1123 date format

---

## Deferred (not testable / out of scope)

| Req | Requirement | Reason |
|-----|-------------|--------|
| 8.2-3 | XML parsers must be namespace-compliant | Implementation detail |
| 8.2-6 | Client must not assume outcome on bad XML response | Client-side rule |
| 9.1-4 | Infinity requests MAY be disabled | Permissive server behavior |
| 9.5-1 | POST on collection is server-defined | No normative behavior to test |
| 9.6-3 | Partial DELETE MAY return 207 | Permissive; hard to trigger deterministically |
| 10.7-1/2 | Timeout header MAY be ignored | Permissive server behavior |
| 11.5 | 507 Insufficient Storage | Server resource condition; not triggerable |

---

## Locking Tests (excluded by default via `--exclude-tag locking`)

All §9.10, §9.11, §10.5, §7.x locking requirements will be covered by tests tagged `locking`. These tests require `MinPrincipals: 2` for conflict scenarios. Full locking test list to be defined separately.

---

## Test Rename Candidates

| Current ID | Proposed ID | Reason |
|-----------|-------------|--------|
| `rfc4918.propfind-depth-infinity-forbidden` | `rfc4918.propfind-depth-infinity-rejected` | "forbidden" implies a policy choice; "rejected" is more precise — the server returns 403 |
