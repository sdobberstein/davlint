# RFC 6352 — CardDAV: vCard Extensions to WebDAV

**Test file:** `internal/suites/rfc6352/rfc6352.go`
**Tests registered:** 25
**Requirements extracted:** ~120 (~75 directly HTTP-testable)

Coverage statuses:
- `conformance ✓` — all meaningful variations tested
- `lint only` — representative test exists; conformance variants missing
- `not covered` — no test exists
- `deferred` — intentionally out of scope with reason

---

## §3 — General Requirements

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-01 | Server MUST support vCard v3 as address object media type | `rfc6352.put-unsupported-media-type` (indirectly) | lint only |
| R-02 | Server MUST support WebDAV Class 3 (DAV header includes "3") | — | not covered |
| R-05 | Server MUST support strong ETags on address object resources | `rfc6352.getetag-strong` | conformance ✓ |
| R-06 | Server MUST support all address book reports | `rfc6352.addressbook-query-*`, `rfc6352.addressbook-multiget*` | lint only |
| R-07 | Server MUST advertise report support in DAV:supported-report-set | `rfc6352.supported-report-set` | conformance ✓ |
| R-08 | Server SHOULD support vCard v4 | — | not covered |
| R-09 | Server SHOULD support extended MKCOL for address book creation | — | not covered |
| R-10 | Server SHOULD support DAV:current-user-principal-URL | `rfc6764.current-user-principal` (in rfc6764 suite) | lint only |

**Missing tests:**
- `rfc6352.dav-class-3` — OPTIONS returns `3` in DAV header

---

## §5.1 — Address Object Resources

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-11 | Address object MUST contain exactly one vCard component | `rfc6352.put-invalid-vcard` (indirectly) | lint only |
| R-12 | vCard MUST have a UID property | — | not covered |
| R-13 | UID MUST be unique within the collection | `rfc6352.put-uid-conflict` | lint only |
| R-14 | GET for unconvertible media type MUST return `CARDDAV:supported-address-data-conversion` | — | not covered |

**Missing tests:**
- `rfc6352.put-missing-uid` — PUT vCard without UID is rejected
- `rfc6352.get-unsupported-accept` — GET with Accept header for an unsupported conversion type returns precondition error with `CARDDAV:supported-address-data-conversion`

---

## §5.2 — Address Book Collections

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-15 | DAV:resourcetype MUST include `DAV:collection` AND `CARDDAV:addressbook` | `rfc6352.addressbook-resourcetype` | conformance ✓ |
| R-17 | MUST NOT allow nested address book collections | `rfc6352.no-nested-addressbook` | conformance ✓ |

---

## §6.1 — OPTIONS Advertisement

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-18 | OPTIONS MUST include "addressbook" in DAV header | `rfc6352.options-addressbook-token` | lint only |

**Conformance gap:** `options-addressbook-token` tests OPTIONS on the collection URL only. RFC 6352 §6.1 requires "addressbook" in the DAV header on any resource supporting address book features, including address object resources.

**Missing test:**
- `rfc6352.options-addressbook-token-resource` — OPTIONS on an address object resource also returns "addressbook" in DAV header

---

## §6.2.1 — addressbook-description Property

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-20 | Property SHOULD NOT be protected (writable by users) | — | not covered |
| R-22 | SHOULD NOT be returned by PROPFIND allprop | — | not covered |

**Missing tests:**
- `rfc6352.addressbook-description-writable` — PROPPATCH can set addressbook-description (SHOULD)
- `rfc6352.addressbook-description-not-in-allprop` — PROPFIND allprop does not include addressbook-description (SHOULD)

---

## §6.2.2 — supported-address-data Property

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-23 | MUST be a protected property | — | not covered |
| R-25 | Storing unsupported media type MUST fail with `CARDDAV:supported-address-data` | `rfc6352.put-unsupported-media-type` | lint only |
| R-27 | SHOULD NOT be returned by PROPFIND allprop | — | not covered |

**Conformance gap:** `put-unsupported-media-type` tests PUT only. The precondition also applies to COPY and MOVE.

**Missing tests:**
- `rfc6352.supported-address-data-protected` — PROPPATCH on supported-address-data returns 403 (protected)
- `rfc6352.supported-address-data-not-in-allprop` — PROPFIND allprop does not include supported-address-data
- `rfc6352.copy-unsupported-media-type` — COPY of non-vCard resource into addressbook returns `CARDDAV:supported-address-data`

---

## §6.2.3 — max-resource-size Property

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-28 | MUST be a protected property | — | not covered |
| R-30 | Oversized resource MUST fail with `CARDDAV:max-resource-size` | `rfc6352.put-max-resource-size` | lint only |
| R-31 | SHOULD NOT be returned by PROPFIND allprop | — | not covered |

**Missing tests:**
- `rfc6352.max-resource-size-protected` — PROPPATCH on max-resource-size returns 403 (protected)
- `rfc6352.max-resource-size-not-in-allprop` — PROPFIND allprop does not include max-resource-size

---

## §6.3.1 — Extended MKCOL

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-32 | Address book creation via extended MKCOL is RECOMMENDED | — | not covered |

**Missing test:**
- `rfc6352.mkcol-addressbook` — extended MKCOL with `CARDDAV:addressbook` resourcetype creates an address book collection (skip if server returns 405)

---

## §6.3.2.1 — PUT/COPY/MOVE Preconditions

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-39 | `CARDDAV:supported-address-data`: wrong media type MUST be rejected | `rfc6352.put-unsupported-media-type` | lint only |
| R-40 | `CARDDAV:valid-address-data`: malformed vCard MUST be rejected | `rfc6352.put-invalid-vcard` | lint only |
| R-41 | `CARDDAV:no-uid-conflict`: duplicate UID MUST be rejected | `rfc6352.put-uid-conflict` | lint only |
| R-42 | PUT to existing URI MUST NOT change UID to a different value | — | not covered |
| R-43 | `CARDDAV:no-uid-conflict` error SHOULD include DAV:href to conflicting resource | — | not covered |
| R-44 | COPY/MOVE to invalid address book location MUST return `CARDDAV:addressbook-collection-location-ok` | — | not covered |
| R-45 | Resource exceeding max size MUST return `CARDDAV:max-resource-size` | `rfc6352.put-max-resource-size` | lint only |

**Conformance gaps:** R-39, R-40, R-41, R-45 are tested for PUT only. Each precondition also applies to COPY and MOVE.

**Missing tests:**
- `rfc6352.put-uid-change` — PUT to existing URI with a different UID value is rejected
- `rfc6352.uid-conflict-href` — `CARDDAV:no-uid-conflict` error body includes DAV:href identifying the conflicting resource
- `rfc6352.copy-location-ok` — COPY of address book to invalid location returns `CARDDAV:addressbook-collection-location-ok`
- `rfc6352.move-location-ok` — MOVE of address book to invalid location returns `CARDDAV:addressbook-collection-location-ok`

---

## §6.3.2.2 — Non-Standard vCard Properties

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-46 | Server MUST accept and preserve X- properties via PUT/GET roundtrip | `rfc6352.put-nonstandard-props` | conformance ✓ |

---

## §6.3.2.3 — Entity Tags

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-49 | DAV:getetag MUST be a strong ETag (no W/ prefix) | `rfc6352.getetag-strong` | conformance ✓ |
| R-50 | GET response MUST include ETag header | `rfc6352.get-etag-header` | conformance ✓ |
| R-51 | Server SHOULD return strong ETag in PUT response when data is octet-equivalent | `rfc6352.put-etag-response` | conformance ✓ |
| R-52 | MUST NOT return strong ETag in PUT response when stored data differs from submitted | — | not covered |

**Missing test:**
- `rfc6352.put-etag-absent-on-normalization` — PUT where server normalizes the vCard (e.g. v3→v4 conversion) MUST NOT return ETag in response. Difficult to trigger deterministically; may need a server that is known to normalize. Severity: must not.

---

## §7.1.1 — addressbook-home-set Property

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-55 | SHOULD NOT be returned by PROPFIND allprop | `rfc6352.home-set-not-in-allprop` | conformance ✓ |

---

## §7.1.2 — principal-address Property

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-58 | SHOULD NOT be returned by PROPFIND allprop | `rfc6352.principal-address-not-in-allprop` | conformance ✓ |

---

## §8.1 — REPORT Method

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-63 | Server MUST support DAV:expand-property REPORT | — | not covered |

**Missing test:**
- `rfc6352.expand-property-report` — DAV:expand-property REPORT returns 207 (not 403/501)

---

## §8.3 — Collations

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-67 | Server MUST support "i;ascii-casemap" | `rfc6352.query-collation-ascii` | conformance ✓ |
| R-68 | Server MUST support "i;unicode-casemap" | `rfc6352.query-collation-unicode` | conformance ✓ |
| R-70 | Server MUST advertise collations in `CARDDAV:supported-collation-set` | `rfc6352.supported-collation-set` | conformance ✓ |
| R-71 | Default collation (no explicit collation) MUST be "i;unicode-casemap" | — | not covered |
| R-72 | Wildcards MUST NOT appear in collation identifiers | — | not covered |
| R-73 | Unsupported collation MUST return `CARDDAV:supported-collation` precondition | `rfc6352.query-unsupported-collation` | conformance ✓ |

**Missing tests:**
- `rfc6352.query-default-collation` — addressbook-query with no collation attribute behaves as if "i;unicode-casemap" was specified
- `rfc6352.query-wildcard-collation` — addressbook-query with wildcard collation identifier returns `CARDDAV:supported-collation` precondition error

---

## §8.3.1 — supported-collation-set Property

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-74 | MUST be a protected property | — | not covered |
| R-76 | SHOULD NOT be returned by PROPFIND allprop | — | not covered |

**Missing tests:**
- `rfc6352.supported-collation-set-protected` — PROPPATCH on supported-collation-set returns 403
- `rfc6352.supported-collation-set-not-in-allprop` — PROPFIND allprop does not include supported-collation-set

---

## §8.5 — Non-Standard Properties in Reports

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-77 | Server MUST accept non-standard names in CARDDAV:address-data in REPORT | — | not covered |
| R-79 | `CARDDAV:supported-filter` precondition for unsupported filter property | `rfc6352.supported-filter-precondition` | conformance ✓ |

**Missing test:**
- `rfc6352.query-nonstandard-address-data-prop` — addressbook-query requesting a non-standard vCard property in address-data is accepted (server returns available data)

---

## §8.6 — addressbook-query Report

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-80 | `CARDDAV:addressbook-query` REPORT MUST be supported | `rfc6352.addressbook-query-*` | lint only |
| R-82 | Request MUST include Depth header | — | not covered |
| R-83 | Successful response MUST be DAV:multistatus | `rfc6352.addressbook-query-no-filter` | lint only |
| R-84 | Response MUST contain DAV:response for each matching address object | `rfc6352.addressbook-query-prop-filter` | lint only |
| R-85 | Non-existent WebDAV property MUST produce 404 in DAV:propstat | — | not covered |
| R-86 | `CARDDAV:supported-address-data` in REPORT for unsupported media type | — | not covered |
| R-87 | `CARDDAV:supported-filter` precondition | `rfc6352.supported-filter-precondition` | conformance ✓ |
| R-88 | SHOULD identify offending filter in `CARDDAV:supported-filter` error body | — | not covered |

**Missing tests:**
- `rfc6352.query-nonexistent-prop` — addressbook-query requesting a non-existent WebDAV property returns 404 in DAV:propstat for that property
- `rfc6352.query-unsupported-media-type` — addressbook-query requesting unsupported address-data type returns `CARDDAV:supported-address-data`

---

## §8.6.2 — Result Truncation

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-91 | Truncated response MUST use 207 + 507 sub-status for request URI | — | not covered |
| R-92 | 507 response SHOULD include `DAV:number-of-matches-within-limits` in DAV:error | — | not covered |
| R-93 | Server SHOULD include partial results with the 507 response | — | not covered |

**Note:** These require a server that enforces result limits, which can't be triggered deterministically. Tests should verify the structure when a 507 is returned (similar to `rfc6578.client-limit` approach).

---

## §8.7 — addressbook-multiget Report

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-95 | `CARDDAV:addressbook-multiget` REPORT MUST be supported | `rfc6352.addressbook-multiget` | lint only |
| R-97 | Request MUST use Depth:0 | — | not covered |
| R-98 | Successful response MUST be DAV:multistatus | `rfc6352.addressbook-multiget` | lint only |
| R-99 | Response MUST include DAV:response for each referenced href | `rfc6352.addressbook-multiget` | lint only |
| R-100 | Non-existent href MUST produce 404 in DAV:response | `rfc6352.addressbook-multiget-not-found` | conformance ✓ |
| R-101 | `CARDDAV:supported-address-data` precondition in multiget | — | not covered |

**Missing tests:**
- `rfc6352.multiget-depth-nonzero-rejected` — addressbook-multiget with Depth != 0 returns 400
- `rfc6352.multiget-unsupported-media-type` — addressbook-multiget requesting unsupported address-data type returns `CARDDAV:supported-address-data`

---

## §10.5 — Group Prefix Matching

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-107 | prop name without group prefix MUST match properties with any or no prefix | — | not covered |
| R-108 | prop name with group prefix MUST match only exact prefix+name | — | not covered |
| R-109 | prop-filter name without group prefix MUST match any or no prefix | — | not covered |
| R-110 | prop-filter name with group prefix MUST match only exact prefix+name | — | not covered |

**Missing tests:**
- `rfc6352.query-group-prefix-filter` — prop-filter with group prefix (e.g. `ITEM1.EMAIL`) matches only that grouped property
- `rfc6352.query-no-group-prefix-filter` — prop-filter without group prefix matches all grouped and ungrouped instances

---

## §10.5.4 — text-match Collation Defaults

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-111 | Server MUST use the collation from text-match collation attribute | `rfc6352.query-collation-ascii`, `rfc6352.query-collation-unicode` | lint only |
| R-112 | Without collation attribute, server MUST default to "i;unicode-casemap" | `rfc6352.query-default-collation` (missing) | not covered |

---

## §13 — Security

| Req | Requirement | Current Test | Status |
|-----|-------------|--------------|--------|
| R-118 | Servers MUST enforce authentication/authorization | `rfc6352.unauthenticated-access-denied` | lint only |
| R-120 | Address books MUST NOT be accessible by unauthenticated users | `rfc6352.unauthenticated-access-denied` | lint only |

**Conformance gap:** `unauthenticated-access-denied` tests only unauthenticated PROPFIND. GET, PUT, DELETE, and REPORT without credentials also need to return 401/403.

**Missing tests:**
- `rfc6352.unauthenticated-get-denied` — unauthenticated GET on address object returns 401/403
- `rfc6352.unauthenticated-put-denied` — unauthenticated PUT returns 401/403
- `rfc6352.unauthenticated-report-denied` — unauthenticated REPORT returns 401/403

---

## Deferred

| Req | Requirement | Reason |
|-----|-------------|--------|
| R-03 | WebDAV ACL support | ACL tests excluded from bundle by default; tagged `acl` |
| R-04 | TLS support | TLS-level test; out of scope for HTTP black-box testing |
| R-33 | Server MAY restrict address book creation locations | Internal server policy; not observable |
| R-47 | Server MAY reject changes to its own private X- properties | Permissive; server-specific |
| R-52 | MUST NOT return strong ETag when data differs from submitted | Only observable when server normalizes data; difficult to trigger deterministically |
| R-62 | Server MAY advertise reports on ordinary collections | Permissive |
| R-90 | Server MAY truncate results | Permissive; structure tested if/when truncation occurs |
| R-105/106 | CR omission and partial vCard validity in responses | Formatting/rendering detail |

---

## Test Rename Candidates

| Current ID | Proposed ID | Reason |
|-----------|-------------|--------|
| `rfc6352.unauthenticated-access-denied` | `rfc6352.unauthenticated-propfind-denied` | Current name implies all methods are tested; only PROPFIND is |

---

## Coverage Summary

| Status | Count |
|--------|-------|
| conformance ✓ | 10 |
| lint only | 15 |
| not covered (testable) | ~30 |
| deferred | ~10 |

The existing 25 tests cover the happy path well and all named preconditions for PUT. The largest gaps are: precondition testing for COPY/MOVE, protected property enforcement, group prefix matching, result truncation structure, and unauthenticated access across all methods.
