# RFC 4791 — CalDAV Core

**Suite:** not yet created

Defines CalDAV: calendar collections, calendar object resources (iCalendar), MKCALENDAR, the calendar-query, calendar-multiget, and free-busy-query REPORTs, PUT preconditions, collations, and ETags on calendar resources.

## Deferred

| Requirement | Reason |
|-------------|--------|
| WebDAV ACL (R-03, R-41–R-44) | Tagged `acl` — excluded by default |
| TLS (R-04) | TLS-level; out of scope |
| `calendar-home-set` on principal resources | ACL principal property; not testable without ACL support |
| Clients SHOULD use `If-None-Match: *` when creating (S-01) | Client-side recommendation |
| MAY support `limit-recurrence-set`, `expand`, `limit-freebusy-set` in calendar-data | MAY-level partial retrieval options |
| MAY support X- component/property names in filters | Server MAY reject — failure mode is correct server behavior |
| `CALDAV:calendar-description` property | MAY be protected; property existence optional |

## Gaps

No tests yet. Priority areas when the suite is created:

| Area | Priority |
|------|----------|
| OPTIONS: `calendar-access` in DAV header | First — gate for all other tests |
| Calendar collection resourcetype (`DAV:collection` + `CALDAV:calendar`) | High |
| MKCALENDAR: creates collection, atomicity, property failure, bad body | High |
| PUT iCal: creates (201), updates (204), GET round-trip | High |
| PUT preconditions: valid-calendar-data, valid-calendar-object-resource, no-uid-conflict, supported-calendar-data | High |
| ETags on calendar resources: present on GET, strong, changes after PUT | High |
| `DAV:supported-report-set` on calendar collection | High |
| calendar-query: all-events, VEVENT filter, prop-filter, empty result, calendar-data response | Medium |
| calendar-multiget: single href, multiple hrefs, missing href → 404 | Medium |
| free-busy-query: returns VFREEBUSY, aggregates events | Medium |
| Collations: ascii-casemap and octet supported, unsupported → CALDAV:supported-collation | Medium |
| No nested calendar collections | Medium |
